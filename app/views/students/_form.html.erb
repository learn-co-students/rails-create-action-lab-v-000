<h4>Student Form</h4>

<%= form_tag students_path do %>
    <h2>First Name:</h2><%= text_field_tag(:first_name) %>
    <h2>Last Name:</h2><%= text_field_tag(:last_name) %>
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <%= submit_tag("Submit Student") %>
<% end %>